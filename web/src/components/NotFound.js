const NotFound = () => {
    const styles = {
        margin: "5rem 1rem",
        fontSize: "1.5rem",
        textAlign: "center"
    }
    return (
        <p style={styles}>Oops! This page does not exist.</p>
    );
}

export default NotFound;  